name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  web-quality:
    name: Web quality
    runs-on: ubuntu-22.04
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify repository contracts
        run: pnpm run verify

      - name: Check formatting
        run: pnpm run format:check

      - name: Lint source
        run: pnpm run lint

      - name: Type-check workspace
        run: pnpm run typecheck

      - name: Run unit tests
        run: pnpm run test

      - name: Build frontend
        run: pnpm run build

  native:
    name: Native (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, macos-15, windows-2025]
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Install Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Restore Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/src-tauri

      - name: Check Rust formatting
        if: runner.os == 'Linux'
        run: cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml -- --check

      - name: Lint Rust
        if: runner.os == 'Linux'
        run: cargo clippy --manifest-path apps/desktop/src-tauri/Cargo.toml --all-targets -- -D warnings

      - name: Run native tests
        run: cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml

      - name: Build native application
        run: pnpm --filter @queryx/desktop tauri build --no-bundle

  postgres-contract:
    name: PostgreSQL contract
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: queryx_test
          POSTGRES_USER: queryx
          POSTGRES_PASSWORD: local-test-only
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U queryx -d queryx_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      QUERYX_TEST_POSTGRES_DATABASE: queryx_test
      QUERYX_TEST_POSTGRES_HOST: localhost
      QUERYX_TEST_POSTGRES_PORT: 5432
      QUERYX_TEST_POSTGRES_USERNAME: queryx
      QUERYX_TEST_POSTGRES_PASSWORD: local-test-only
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Linux system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev build-essential curl wget file libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Restore Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/src-tauri

      - name: Run live PostgreSQL contract tests
        run: cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml postgres_driver

  mysql-contract:
    name: MySQL contract
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    services:
      mysql:
        image: mysql:8.4
        env:
          MYSQL_DATABASE: queryx_test
          MYSQL_USER: queryx
          MYSQL_PASSWORD: local-test-only
          MYSQL_ROOT_PASSWORD: root-local-test-only
        ports:
          - 3306:3306
        options: >-
          --health-cmd "mysqladmin ping -h 127.0.0.1 -uqueryx -plocal-test-only"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      QUERYX_TEST_MYSQL_DATABASE: queryx_test
      QUERYX_TEST_MYSQL_HOST: 127.0.0.1
      QUERYX_TEST_MYSQL_PORT: 3306
      QUERYX_TEST_MYSQL_USER: queryx
      QUERYX_TEST_MYSQL_PASSWORD: local-test-only
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Restore Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/src-tauri

      - name: Run live MySQL contract tests
        run: cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml mysql_driver

  mariadb-contract:
    name: MariaDB contract
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    services:
      mariadb:
        image: mariadb:11.4
        env:
          MARIADB_DATABASE: queryx_test
          MARIADB_USER: queryx
          MARIADB_PASSWORD: local-test-only
          MARIADB_ROOT_PASSWORD: root-local-test-only
        ports:
          - 3307:3306
        options: >-
          --health-cmd "healthcheck.sh --connect --innodb_initialized"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 10
    env:
      QUERYX_TEST_MYSQL_DATABASE: queryx_test
      QUERYX_TEST_MYSQL_HOST: 127.0.0.1
      QUERYX_TEST_MYSQL_PORT: 3307
      QUERYX_TEST_MYSQL_USER: queryx
      QUERYX_TEST_MYSQL_PASSWORD: local-test-only
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Restore Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/src-tauri

      - name: Run live MariaDB contract tests
        run: cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml mysql_driver

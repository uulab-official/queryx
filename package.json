{
  "name": "queryx",
  "private": true,
  "version": "0.1.0",
  "description": "Open-source local-first database IDE",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/uulab-official/queryx.git"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "pnpm --filter @queryx/desktop dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run",
    "test:native": "cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "check:native": "cargo fmt --manifest-path apps/desktop/src-tauri/Cargo.toml -- --check && cargo clippy --manifest-path apps/desktop/src-tauri/Cargo.toml --all-targets -- -D warnings",
    "verify": "node scripts/verify-repo.mjs",
    "lint": "pnpm exec biome lint apps/desktop/src packages/core/src packages/shared/src scripts",
    "format": "pnpm exec biome format --write apps/desktop/src packages/core/src packages/shared/src scripts",
    "format:check": "pnpm exec biome format apps/desktop/src packages/core/src packages/shared/src scripts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "vitest": "^2.1.8"
  }
}
